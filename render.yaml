services:
  - type: web
    name: peaceful-nights-guide
    runtime: static
    # Plain HTML/CSS/JS site — there is nothing to compile.
    buildCommand: ""
    # Serve the files straight from the repo root.
    staticPublishPath: .
